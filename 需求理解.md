# 需求理解文档

> 项目：企业级权限管理系统（合同审批流）  
> 技术栈：Spring Boot 2.7.3 + Vue 3（Element Plus）+ MyBatis-Plus
> 作者：张荣炜  
> 日期：2026-07-09

---

## 1. 需求复述

### 1.1 一句话概括

为一家多部门、多角色的企业，从零构建一套**企业级权限管理系统**，以"合同审批流"为核心业务场景落地。权限须覆盖三条主线：**功能/操作权限、数据/行级范围、UI 可见性**，并驱动一条严格顺序的审批流（发起 → 合同管理员处理 → 部门经理审批 → 领导审批 → 完成）。

### 1.2 业务全景

**组织结构**：公司有若干部门（扁平，无上下级层级），员工有主部门，可兼任多个部门。

**核心业务单据**：合同（Contract），包含 10 个业务字段（合同编号、名称、客户、金额、条款明细、附件、备注、对方银行账号、归属部门、申请人），加上系统字段（状态、当前审批节点、审批记录、时间戳等）。

**四个角色**：

| 用户 | 角色 | 功能权限 | 数据范围 |
|------|------|----------|----------|
| U1 | 申请人（普通员工） | contract:create, contract:view, contract:edit, contract:withdraw | 仅本人 |
| U2 | 合同管理员 | contract:view, contract:process | 自定义{若干部门} |
| U3 | 部门经理 | contract:view, contract:approve | 本部门 |
| U4 | 领导 | contract:view, contract:approve, contract:delete | 全部 |

**审批流**：U1 发起 → U2 处理 → U3 审批 → U4 审批 → 完成。严格按序，不可越级/跳节点。

**种子数据**：4 个部门（研发部、运营部、市场部、财务部），4 个用户，若干合同分布在不同部门和审批节点。

### 1.3 必做需求清单

1. **RBAC 基线（含角色层级继承）**：用户-角色-权限多对多，领导继承部门经理的功能权限。
2. **两轴分离**：功能权限与数据范围各自独立建表。
3. **行级数据范围**：仅本人 / 本部门 / 自定义若干部门 / 全部，四档。
4. **权限驱动 UI**：动态菜单/路由 + 按钮指令（移除 DOM），共用权限码。
5. **合同审批工作流**：状态机建模 + 节点按序推进 + 功能权限与当前节点双重门控。
6. **集中审计日志**：who/what/when/outcome。
7. **字段级查看与脱敏配置**：按审批环节配置，每个字段可配 可见/脱敏/隐藏，数据驱动、非硬编码。

### 1.4 验收场景（10 个）

| # | 维度 | 场景 | 期望 |
|---|------|------|------|
| 1 | 数据范围 | U1 查询合同列表 | 仅自己创建的合同 |
| 2 | 数据范围 | U2 查询 | 自定义部门集合内的合同 |
| 3 | 数据范围 | U3 查询 | 本部门（研发部）的合同 |
| 4 | 数据范围 | U4 查询 | 全部合同 |
| 5 | 功能权限 | U1 列表 | 无"处理/审批/删除"按钮（DOM 移除） |
| 6 | 工作流 | U1→U2→U3→U4 完整走完 | 按序四个节点到达终态 |
| 7 | 节点顺序 | U3 直接审批未到部门经理节点的合同 | 不可操作 / 403 |
| 8 | 审计 | 各节点操作后 | 审计表有完整记录 |
| 9 | 字段级 | 领导审批环节看详情 | 不见条款明细/附件/备注/对方银行账号 |
| 10 | 字段配置可变 | 改领导审批环节配置加"附件可见" | 刷新后能看到附件 |

---

## 2. 隐含难点与边界分析

### 2.1 两轴分离（核心难点）

**问题本质**：功能权限（能不能做）和数据范围（能碰哪些行）是正交的两个维度。如果把它们塞进同一个模型（比如在角色表里加一个 `data_scope` 字段），会导致：

- **角色爆炸**：每个"功能权限组合 × 数据范围组合"都需要一个独立角色。比如"部门经理-看本部门"和"部门经理-看全部"就得是两个角色。
- **权限混淆**：修改数据范围时需要改角色定义，影响所有拥有该角色的用户。
- **不可扩展**：新增一种数据范围类型（如"本部门 + 关联部门"）需要改角色表结构。

**我的解法**：

```
角色表 (role) ──多对多── 权限表 (permission)     ← 功能权限轴
角色表 (role) ──1对1─── 数据范围配置 (data_scope) ← 数据范围轴
```

- 功能权限：`role_permission` 中间表，存 `contract:view`、`contract:approve` 等权限码。
- 数据范围：`role_data_scope` 表，存 `scope_type`（SELF / DEPT / CUSTOM / ALL）+ `dept_ids`（当 scope_type=CUSTOM 时）。
- **决策时求交**：权限检查 = `hasPermission(user, action)` ∧ `applyDataScope(user, query)`。先判断用户能否执行该操作，再对 SQL 查询施加数据范围过滤。
- **多角色/兼任取并集**：用户拥有多个角色时，功能权限取所有角色权限码的并集，数据范围取所有角色数据范围的并集（SQL 层面用 OR 拼接）。

### 2.2 三层强制（核心难点）

**问题本质**：安全不能只靠前端隐藏。必须三层都做：

| 层 | 位置 | 作用 | 技术手段 |
|----|------|------|----------|
| 第1层 | 菜单/路由 | 控制用户能看到哪些页面 | 后端动态路由表 + Vue Router `addRoute` |
| 第2层 | 按钮指令 | 控制页面内按钮显隐 | 自定义指令 `v-permission`，移除 DOM |
| 第3层 | 后端鉴权 | 真正的安全防线 | Spring Security 方法级注解 + AOP 拦截器 |

**关键点**：
- 三层共用一套权限码词表（如 `contract:approve`），由后端统一维护。
- 前端隐藏不是安全——即使前端没显示按钮，用户仍可通过 curl/Postman 直接调 API，所以后端必须独立鉴权。
- **deny-by-default**：所有接口默认拒绝访问，只有显式配置了权限的用户才能访问。Spring Security 默认行为是 authenticated 即可访问，需显式配置 `@PreAuthorize`。

**我的解法**：

```
前端：路由守卫 → 从后端获取用户权限码集合 → 动态注册路由
      按钮：v-permission 指令 → 检查权限码是否在集合中 → 不在则 removeChild
后端：Spring Security FilterChain → JWT 认证 → @PreAuthorize("hasAuthority('contract:approve')")
      全局异常处理 → 403/404 统一返回
      数据范围：MyBatis-Plus DataPermissionInterceptor / SQL 改写
```

### 2.3 IDOR 与越权防护（加分项，但架构上必须预留）

**问题本质**：IDOR（Insecure Direct Object Reference，不安全直接对象引用）是 OWASP API1:2023 排名第一的安全风险。用户通过修改 URL 或请求参数中的 ID（如 `contractId`、`departmentId`），可能访问到不属于自己的资源。

**关键边界**：

1. **带 ID 的端点越权应返回 404，而非 403**：
   - 403 = "我知道这个资源存在，但你没权限访问" → 泄露存在性信息。
   - 404 = "这个资源不存在（或你没权限知道它是否存在）" → 不泄露信息。
   - 实现：先查数据，再判断权限，无权限时抛出 404 而非 403。或者在查询时直接施加数据范围，查不到就返回 404。

2. **越权防护的三个层次**：
   - 功能权限：检查用户是否有 `contract:approve` 权限码。
   - 对象级权限：检查用户是否有权访问**这个特定合同**（通过数据范围）。
   - 参数篡改防护：检查 `departmentId` 参数是否在用户数据范围内。

**我的解法**：

```java
// 伪代码
@GetMapping("/contract/{id}")
@PreAuthorize("hasAuthority('contract:view')")
public Result<ContractVO> getContract(@PathVariable Long id) {
    // 查询时已经施加数据范围，查不到 = 不存在/无权限 → 404
    Contract contract = contractService.getByIdWithScope(id);
    if (contract == null) {
        throw new NotFoundException("合同不存在");
    }
    return Result.ok(contract);
}
```

数据范围在 SQL 层施加，查询本身就过滤了无权访问的行，因此查不到时自然返回 404。

### 2.4 节点顺序（核心难点）

**问题本质**：审批流有严格的顺序约束，合同只能被**当前节点对应角色**的用户推进。不能越级、不能跳节点。这是一个**状态机 + 角色门控**的复合问题。

**边界场景**：
- U3 在合同还在 U2 节点时尝试审批 → 403。
- U1 在合同已提交后尝试撤回 → 需要检查当前节点是否允许撤回。
- 同一节点有多个用户时，谁操作算推进？——本需求中每个角色对应一个用户，暂不涉及多人竞争。
- 回退/驳回是否支持？——需求未明确，但状态机应预留扩展。

**我的解法**：

采用**有限状态机（FSM）**建模：

```
状态：DRAFT → PENDING_ADMIN → PENDING_MANAGER → PENDING_LEADER → APPROVED
操作：submit → process → approve(dept) → approve(leader)
```

每个操作绑定两个条件：
1. **功能权限**：当前用户是否有对应的权限码（如 `contract:approve`）。
2. **节点门控**：合同当前状态是否允许该操作（状态机转换是否合法）。

```java
// 伪代码：审批操作
@PreAuthorize("hasAuthority('contract:approve')")
public void approve(Long contractId) {
    Contract contract = findById(contractId);
    // 节点门控：检查当前状态是否允许审批
    if (contract.getStatus() != ContractStatus.PENDING_MANAGER) {
        throw new BusinessException("当前合同不在部门经理审批节点");
    }
    // 数据范围：检查该合同是否在当前用户的数据范围内
    // （通过 SQL 层自动施加，查不到即抛异常）
    contract.setStatus(ContractStatus.PENDING_LEADER);
    // 记录审计日志
}
```

**状态机表设计**：`approval_flow_config` 表存储状态转移规则，数据驱动，方便扩展。

### 2.5 字段级可见性与脱敏（难点）

**问题本质**：合同在不同审批环节，可见字段不同。这是**流程级配置**，不是角色权限。每个字段可配置为：可见 / 脱敏（部分隐藏）/ 隐藏。

**关键边界**：
- 字段级配置是**数据驱动**的，修改配置后刷新即可生效，不需要改代码。
- 脱敏与隐藏是不同的：脱敏 = 字段仍在但值被变换（如 `138****1234`），隐藏 = 字段从 JSON 中消失。
- 必须**在所有 JSON 出口一致生效**：列表、详情、导出。

**我的解法**：

```sql
-- 字段可见性配置表
CREATE TABLE field_visibility_config (
    id BIGINT PRIMARY KEY,
    stage VARCHAR(32) NOT NULL,       -- 审批环节: PENDING_ADMIN, PENDING_MANAGER, PENDING_LEADER
    field_name VARCHAR(64) NOT NULL,  -- 字段名: contract_amount, bank_account, etc.
    visibility VARCHAR(16) NOT NULL,  -- VISIBLE / MASKED / HIDDEN
    UNIQUE(stage, field_name)
);
```

在序列化时，通过 Jackson 自定义序列化器或 AOP 环绕通知，根据当前合同状态查询字段配置，动态过滤/脱敏字段。

```java
// 伪代码
public ContractVO toVO(Contract contract) {
    ContractVO vo = BeanUtils.copy(contract);
    String stage = contract.getStatus().name();
    List<FieldConfig> configs = fieldConfigService.getByStage(stage);
    for (FieldConfig cfg : configs) {
        if (cfg.isHidden()) {
            vo.setFieldNull(cfg.getFieldName());  // Jackson @JsonInclude(NON_NULL) 自动隐藏
        } else if (cfg.isMasked()) {
            vo.maskField(cfg.getFieldName());      // 如金额显示为 "****"
        }
    }
    return vo;
}
```

### 2.6 多角色/兼任取并集

一个用户可能拥有多个角色（如既是部门经理又是合同管理员），功能权限和数据范围都取**并集**。

**功能权限并集**：`Set<Permission> = role1.permissions ∪ role2.permissions`

**数据范围并集**：SQL 层面用 OR 拼接多个角色的数据范围条件。

```sql
-- 用户有角色A（本部门）和角色B（自定义{研发部,市场部}）
-- 数据范围并集 = 本部门 OR 部门在{研发部,市场部}
WHERE (
    contract.applicant_id = #{currentUserId}          -- 角色A: 仅本人
    OR contract.dept_id IN (#{roleADeptIds})          -- 角色A: 本部门
    OR contract.dept_id IN (#{roleBCustomDeptIds})     -- 角色B: 自定义部门
)
```

---

## 3. 总体解题思路

### 3.1 技术选型

| 层 | 技术 |
|----|------|
| 后端框架 | Spring Boot 2.7.3 + Spring Security |
| ORM | MyBatis-Plus（数据范围拦截器成熟） |
| 数据库 | MySQL 8.0（H2 内存库用于 demo） |
| 前端框架 | Vue 3 + Vite + Element Plus |
| 路由 | Vue Router 4（动态路由） |
| 状态管理 | Pinia |
| 认证 | JWT（无状态，适合 demo） |

### 3.2 分层架构

```
前端 (Vue 3)
  ├── 路由守卫 → 动态路由（第1层）
  ├── v-permission 指令 → 按钮显隐（第2层）
  └── API 请求 → axios 拦截器附加 JWT

后端 (Spring Boot)
  ├── Filter: JWT 认证过滤器
  ├── Interceptor: 数据范围拦截器（MyBatis-Plus）
  ├── AOP: 审计日志切面
  ├── @PreAuthorize: 方法级鉴权（第3层）
  ├── Service: 权限引擎 + 审批流状态机 + 字段可见性
  └── Repository: MyBatis-Plus + 参数化 SQL
```

### 3.3 开发顺序

1. 数据模型设计（ER 图 → 建表 SQL）
2. RBAC 权限引擎（用户-角色-权限 + 角色继承）
3. 数据范围引擎（四档 + SQL 改写）
4. 审批流状态机（状态定义 + 转移规则 + 节点门控）
5. 字段可见性引擎（配置驱动 + 序列化过滤）
6. 审计日志（AOP 自动记录）
7. 后端 API（CRUD + 审批流 API）
8. 前端（登录 + 动态路由 + 合同列表/详情 + 审批操作）
9. 种子数据 + 验收场景测试

---

## 4. 风险与边界总结

| 风险/边界 | 影响 | 缓解措施 |
|-----------|------|----------|
| 两轴混在一起 | 角色爆炸、不可维护 | 独立建表，决策时求交 |
| 前端隐藏当安全 | 越权漏洞 | 三层强制，后端独立鉴权 |
| 直接返回 403 | 泄露资源存在性 | 数据范围在 SQL 层施加，查不到返回 404 |
| 硬编码节点顺序 | 审批流变更需改代码 | 状态机配置表驱动 |
| 字段级硬编码 | 每次调整需发版 | 配置表 + 动态序列化 |
| 多角色权限冲突 | 范围取交集导致数据丢失 | 明确规定取并集 |
| 忘记 deny-by-default | 未配置权限的接口默认放行 | 全局配置 + CI 门禁检查 |